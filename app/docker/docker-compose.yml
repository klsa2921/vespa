version: '3.8'

services:
  vespa:
    image: vespaengine/vespa:latest
    container_name: vespa
    ports:
      - "8080:8080"    # HTTP API
      - "19071:19071"  # Admin interface
    volumes:
      - ./app:/app
      - ./vespa-data:/opt/vespa/var
      - ./vespa-logs:/opt/vespa/logs
